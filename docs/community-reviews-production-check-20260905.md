# 买家秀正式连接与连续刷新复查（2026-09-05）

正式后台 `https://admin.decomkt.com`，Macfox Bike（组织 1 / 店铺 3），Shopify `macfoxebike.myshopify.com`。

## 已完成

- 电脑连接恢复后，后台界面已显示 Community Reviews 已连接，762 条四星/五星评论、5 个车型文件夹、50 张素材、统一 Read more 与展示开关配置正常。未重复安装或导入。
- 后台随机预览显示真实评价，图片和底部车型对应；拖动轮播正常，没有左右箭头。通过 View Bike 链接打开 X1S，实际到达 `https://macfoxbike.com/products/macfox-x1`。此轮链接跳转使用键盘激活验证，不能单凭此结果声称鼠标点击体验已完成复查。
- 正式代理首次三批验证共 36 条不同评论，但图片只有 29 张不同。原因是原选择器按评论逐条匹配，即使其他车型还有新素材，也可能提前回收某热门车型的已展示图片。
- 后端改为先遍历全部车型中的新评论/新图片组合，再分阶段放宽历史限制；单批仍不重复、不跨车型错配。素材有限时保留可控回收，不能保证无限刷新永不重复。
- 测试服务器隔离测试：8 项、97 断言通过，新增覆盖评论分布倾斜且部分车型图片稀少的连续三批测试。真实测试服务器签名代理三批各 8 张，共 24 张不同图片。测试店公开店面返回访问页面，未把 HTML 当作成功数据。
- 正式后端已更新为 `decoadmin-app:cr-refresh-20260905-production` / `decoadmin-nginx:cr-refresh-20260905-production`。测试镜像为同前缀 `-staging`。本次只改后端选图，不需要重复发布未修改的 Shopify 扩展。

## 主题进度

- 正式当前主题：`193157726573`，名称 `Macfox Labor Day 产品比较模块调整`。
- 新建完整备份主题 `193271431533`，与原有副本 `193193443693` 区分。已提交重命名为 `备份 2026-09-05 买家秀上线前`。
- 设计图对应 `/collections/electric-bike` 使用独立 `electric-bike` 模板，原来是 `Custom Collections Community` 手动评价区，有左右按钮。
- 模板已有 25/25 分区，因此在未保存编辑中移除原评价分区，在相同可见位置添加正式 `Community Reviews` App 区块。其余隐藏的 Demo 分区和其他内容保持原样。新分区预览正常、浅色背景、紫色星级。
- **尚未保存上线**：准备手机预览时 Mac 再次锁屏，已请求手动解锁。不能把主题编辑器中的预览称为正式商城已上线。

## 原计划（已被下方用户范围更正取代，不再执行正式主题操作）

1. 正式三批复查已通过：每批 12 张，共 36 条不同评论、36 张不同图片。
2. 解锁后复查手机布局与鼠标链接，再保存当前主题的 `electric-bike` 模板。
3. 在公开集合页确认仅有一个新评价区，旧左右箭头区域已移除；检查接近视口加载和正常产品跳转。

## 用户范围更正与撤销（2026-09-05）

用户明确要求不影响、不改动 Shopify Macfox Bike 正式店铺；所有插件测试仅限专用 macfox-test-app。已通过主题编辑器“退出”及“离开页面”确认放弃全部未保存更改，返回主题列表，没有点击保存或发布。后续不再推进正式店铺主题添加。此前已完成的正式后台配置（50 张素材、5 个车型关联）与已创建的备份主题不擅自删除或更改。

## 测试店交互收尾（2026-09-05）

- 本轮只操作 Shopify `macfox-test-app` 的 `Deco Smart Cart Test` 主题（165279695096）预览，未保存主题配置，未发布或重启正式服务。
- Shopify 手机宽度预览中，评价卡片文字、星级、Read more、车型缩略图及 View Bike 排版完整，评价轮播没有左右切换按钮。此项为浏览器手机宽度预览，不是实体手机触控测试。
- 手机宽度预览横向拖动后，卡片经过惯性移动停在非整页位置；鼠标点击 M16 的 View Bike，预览地址及商品标题均切换到测试店 `/products/macfox-m16-ebike`。
- 桌面预览从 View Bike 链接位置起拖能移动轮播且不会误跳转；随后直接点击 X7 的 View Bike，实际到达测试店 `/products/macfox-x7`。
- 在首条 Demo Alex 处向右拖动，依次出现末尾 Demo Taylor、Demo Jamie 并衔接回 Demo Alex，验证首尾循环，无整页翻页。
- 测试 M16 商品详情显示 Sold out；当前“在售”筛选定义为 ACTIVE、Online Store 已发布及本地启用，不等同于库存可购买。本轮未修改库存。
- 本次只补充本地核验记录，未向正式服务器更新文件。
