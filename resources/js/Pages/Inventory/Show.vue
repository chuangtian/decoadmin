<script setup lang="ts">import{Head,Link}from'@inertiajs/vue3';import AppLayout from'../../Layouts/AppLayout.vue';interface Level{id:number;available:number;updated_at_shopify:string|null;synced_at:string|null;location:{id:number;name:string;active:boolean;address:Record<string,string|null>|null}}interface Item{id:number;sku:string|null;tracked:boolean;shopify_inventory_item_id:string;synced_at:string|null;variant:{title:string;product:{title:string;handle:string}|null}|null;levels:Level[]}defineProps<{inventoryItem:Item}>();</script>
<template><Head title="库存详情"/><AppLayout :breadcrumbs="[{label:'业务中心'},{label:'库存管理',href:'/inventory'},{label:inventoryItem.sku||'库存详情'}]"><div class="mx-auto max-w-5xl space-y-6"><Link href="/inventory" class="text-sm font-semibold text-slate-500">← 返回库存列表</Link><header><p class="text-sm font-semibold text-emerald-700">Shopify 库存项目</p><h1 class="mt-1 text-3xl font-semibold text-slate-950">{{inventoryItem.variant?.product?.title||'未关联商品'}}</h1><p class="mt-2 text-sm text-slate-500">{{inventoryItem.variant?.title||'未知变体'}} · SKU {{inventoryItem.sku||'—'}}</p></header><section class="grid gap-4 sm:grid-cols-3"><div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p class="text-xs text-slate-400">跟踪库存</p><p class="mt-2 font-semibold">{{inventoryItem.tracked?'是':'否'}}</p></div><div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p class="text-xs text-slate-400">地点数量</p><p class="mt-2 font-semibold">{{inventoryItem.levels.length}}</p></div><div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p class="text-xs text-slate-400">总可用库存</p><p class="mt-2 font-semibold">{{inventoryItem.levels.reduce((sum,level)=>sum+level.available,0)}}</p></div></section><section class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="border-b border-slate-200 px-6 py-5"><h2 class="font-semibold">地点库存</h2></div><div class="divide-y divide-slate-100"><article v-for="level in inventoryItem.levels" :key="level.id" class="flex items-center justify-between gap-4 px-6 py-5"><div><p class="font-semibold text-slate-900">{{level.location.name}}</p><p class="mt-1 text-xs text-slate-400">{{level.location.active?'启用地点':'停用地点'}}</p></div><p class="text-2xl font-semibold text-slate-950">{{level.available}}</p></article></div></section></div></AppLayout></template>
