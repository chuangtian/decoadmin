{
  "name": "instagram-feed",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "private": true,
  "workspaces": [
    "extensions/*"
  ],
  "scripts": {
    "shopify": "shopify",
    "build": "npm run build:local",
    "build:local": "shopify app build --config local",
    "build:test": "shopify app build --config test",
    "build:production": "shopify app build --config production",
    "dev": "shopify app dev --config local",
    "check": "npm run check:project && npm run build:local",
    "check:project": "node scripts/validate-project.mjs",
    "check:config": "shopify app config validate --config local --json",
    "check:config:test": "shopify app config validate --config test --json",
    "check:config:production": "shopify app config validate --config production --json",
    "config:link:test": "shopify app config link --config test",
    "config:link:production": "shopify app config link --config production",
    "config:use": "shopify app config use",
    "info": "shopify app info --config local",
    "generate": "shopify app generate",
    "deploy:local": "shopify app deploy --config local",
    "deploy:test": "shopify app deploy --config test",
    "deploy:production": "shopify app deploy --config production"
  },
  "dependencies": {},
  "devDependencies": {},
  "trustedDependencies": [
    "@shopify/plugin-cloudflare"
  ],
  "author": "tianchuang"
}
