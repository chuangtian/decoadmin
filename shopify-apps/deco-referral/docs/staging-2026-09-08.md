# Referral backend staging verification — 2026-09-08

## Scope

- Backend: `https://testadmin.decomkt.com`, `/opt/decoadmin/staging`, `.env.staging`, Compose project `decoadmin-staging`.
- Authorized store: `macfox-test-app.myshopify.com`, backend Store 1 / Organization 1.
- Backend source commit: `e39bc26`; cumulative Referral changes only, based on `16d8274`.
- No production deployment, Shopify App release, installation, or Shopify mutation was performed.

## Release

The app and nginx services use `decoadmin-app:referral-20260908-staging` and `decoadmin-nginx:referral-20260908-staging`. The release script explicitly exports these image selections; `.env.staging` was not changed. Future Compose operations must select the intended image tags explicitly rather than assume they are persisted in the environment file.

The three Referral migrations were applied. App and nginx were rebuilt and restarted. Horizon and scheduler retained their existing images; this release introduces no Referral background jobs.

Release files and pre-deployment database/core-file backups are retained on the test server at `/opt/decoadmin/staging/.releases/referral-20260908`. Previous web images were `decoadmin-app:16d8274-product-ui-staging` and `decoadmin-nginx:16d8274-product-ui-staging`. Database backups contain private data and must remain protected on the server.

## Evidence

- Local feature suite: 8 tests, 81 assertions passed; PHP formatting passed.
- Installation query validated against Shopify Admin GraphQL 2026-07 schema.
- Staging application, database, and Redis health checks passed.
- Staging schema exists; frontend production build succeeded and manifest is present.
- Staging guard rejects another shop; configured environment is `staging`.
- Transactional staging smoke test passed: create/activate plan, approve promoter, generate link and pending coupon, resolve test-store redirect target, suspend membership and reject its link.
- All smoke-test business and audit data were rolled back. The smoke test made zero Shopify requests and did not follow storefront redirects.
- Public bootstrap endpoint without a bearer token returned HTTP 401; unauthenticated admin page returned HTTP 302.

## Outstanding verification and implementation

Referral-specific test credentials are not configured. Actual Shopify installation/token exchange has only been tested with mocked responses. No coupon has been created in Shopify.

Browser automation timed out, so authenticated visual verification is outstanding. Service-level smoke tests do not substitute for this check.

Discount synchronization, checkout attribution, order processing, ledgers/refunds, payouts, and customer-facing portal remain unfinished. The deployed foundation is not a complete operational affiliate program.
