# Deco Personalization App Home

This Admin UI extension checks the signed-in shop, verifies its DecoAdmin Commerce Hub connection, bootstraps the independent Personalization installation, and links to the store-scoped DecoAdmin workspace.

It is Test-only. It contains no Shopify credentials, no Local runtime, no Production origin, and no store allow-list.
